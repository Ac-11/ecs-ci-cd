version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: ""  # Updated ARN will go to the S3 bucket.
        LoadBalancerInfo:
          ContainerName: "nodejs-container"
          ContainerPort: 3000
