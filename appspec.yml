version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: "${{ steps.task-def.outputs.updated_task_def_arn }}"  # Updated ARN
        LoadBalancerInfo:
          ContainerName: "nodejs-container"
          ContainerPort: 3000
